# TODO

- [x] 프로필 열람 페이지 카드 스와이프 애니메이션
  - [ ] 스와이프 시 뒤 카드 미리보기
- [ ] 프로필 열람 시 이전 프로필이 제외되도록 수정
- [ ] 오류 처리
  - [ ] 랜덤 프로필 열람 시 프로필이 없는 경우 (404)
  - [ ] 프로필 생성 실패
  - [ ] 닉네임 생성 실패
  - [ ] 사용 가능한 티켓이 없는 경우
  - [ ] 인증번호 발급에 실패한 경우
  - [ ] 열람자 정보 가져오기에 실패한 경우
- [ ] 프로필 등록 페이지 validation
- [ ] 이미지 Preload (미리 로드하여 이미지 표시 딜레이 줄이기)
- [x] 이미지 압축
- [ ] AI로 인해 중복된 div 제거, 로직 분리
- [ ] 애니메이션 개선
  - [ ] 페이지 전환 애니메이션
  - [ ] 프로필 열람 시 애니메이션
  - [ ] 이미지 에셋 애니메이션
  - [ ] 프로필 등록 애니메이션

## 인증 세션

2026-09-10에 "프로필이 있는데 방 생성에서 프로필을 등록하라고 뜬다"는 제보로 조사해 고쳤다(#9, PR #47).

- [x] 토큰 갱신 실패와 세션 만료를 구분하지 않아 계정이 갈렸다 — 401만 거절로 보고 세션을 끝내되 익명 등록은 하지 않는다. 5xx·네트워크는 토큰을 그대로 둔다. 갱신은 `fetch.ts`의 단일비행 하나뿐이다
- [x] 갱신할 때마다 `provider`가 `"local"`로 덮어써졌다 — 안 넘기면 있던 값을 유지한다
- [x] `useAuth`가 화면마다 따로 초기화돼 익명 등록·갱신이 겹쳤다 — Layout 한 곳에서만 부른다. 등록 응답이 이미 있는 토큰을 덮지 않고, 갱신도 그 사이 세션이 바뀌었으면 결과를 버린다
- [x] 구글 세션이 실제로 만료됐을 때의 처리 — 조용히 익명이 되지 않고 세션을 끝낸 뒤 안내한다(google은 "다시 로그인해주세요", local은 "새로고침하면 다시 시작할 수 있어요"). 다음 로드에서 토큰이 아예 없으면 그때 익명으로 시작한다
- [ ] 세션이 끝난 뒤 앱이 새로고침 전까지 멈춘다 — 토스트 한 번이 전부라, 사라진 뒤엔 왜 아무것도 안 되는지 알 길이 없다. 다시 시작·다시 로그인 버튼이 달린 배너로 바꾸고, `local`이면 그 자리에서 새 익명 계정을 만들어 준다
- [ ] 구글 로그인이 기존 익명 계정을 묶는지 실서버로 확인 — 교환 요청 본문이 `code`뿐이라 서버는 Bearer로 누군지 안다고 보고 Bearer를 유지했다(464848c). 프로필·티켓이 있는 익명 계정으로 처음 구글 로그인한 뒤 `/api/users/me`의 uuid가 같은지 봐야 한다
- [ ] 결제 진입 시 인증 상태 사전 검증 — #9의 2번. 만료 30초 전 구간에 결제 버튼이 잠기고 갱신을 부를 요청이 없어 새로고침 전까지 멈춘다

## 모두의 시그널 (미팅)

미팅 기능 인수인계 시점에 남아 있던 항목 중 아직 해결되지 않은 것들이다.

### 디자인은 있으나 미구현

- [x] 방 상세 바텀시트 — 핀을 누르면 방장 닉네임·초대 문구·참여자 목록·"방 폭파까지 N분"을 보여주고 "참여하기"로 넘긴다 (Figma `2608:13522`)
- [x] 설명서 모달 4단계 — 1/4 모두의 시그널 → 2/4 미팅을 만들거나 직접 신청 → 3/4 무조건 N:N → 4/4 즐기기만 하면 끝 (Figma `2608:15495`~`16023`)
- [x] 매칭 완료 시트 접기/펼치기 — 손잡이를 끌거나 눌러 접으면 연락처·상대 정보가 숨고 버튼만 남는다 (Figma `2608:15087`, `2608:15364`)
- [ ] 친구초대 버튼 동작 — Figma에 대응 화면이 없다. 초대 링크 정책을 먼저 정해야 한다

### 로비 잔여 이슈

- [x] 최신 매칭 배너를 `visibleUntil`로 걷어낸다. 매칭된 순간부터 30초만 띄우고, 폴링에 맡기지 않도록 배너가 자체 타이머를 갖는다
- [x] `SlotMarker`의 남은 시간이 렌더 시점 계산이라 얼어붙는다. 보드 응답이 매 폴링 같아 구조적 공유가 리렌더를 막으므로 포그라운드에서도 멈춘다 — 로비에 틱을 두어 마커들이 공유한다
- [x] 끝난 방은 슬롯에서 빠져 다시 "+" 핀이 된다. 서버가 걷어가기 전 한 폴링 주기 동안은 남으므로 그 사이 클릭을 막는다
- [x] `PartySize`의 "미선택"과 "미정"을 분리한다. 고르지 않았으면 `null`, "미정"을 골랐으면 `"ANY"`
- [x] `ENABLE_REGISTER`가 꺼져 있으면 안내 모달을 띄우지 않고 사유를 토스트로 알린다
- [x] 구매한 프로필 목(`/profiles/me/purchased`)의 연락처를 `@` 형태로 맞춘다. URL이 들어 있어 `ProfileCard`가 전화번호로 오인하고, 누르면 URL을 복사하며 "전화번호가 복사되었어요"를 띄웠다
- [x] `public/mockServiceWorker.js`를 `.prettierignore`에 넣는다. msw 생성 파일이라 `pnpm format`과 서로 덮어쓰며 194줄이 왔다 갔다 했다
- [x] 멤버 표시 조각을 `lib/meeting.ts`로 모은다. 세 곳에 복붙돼 있었고 구분자도 한 곳만 달랐다(`∙` U+2219). 마크업은 행·칩 그대로 둔다

- [x] 끝난 방으로는 참여 폼이 열리지 않게 한다. 서버가 끝난 방도 200으로 내려주므로 조회 성공만으로는 참여 가능하다는 뜻이 아니다

### 백엔드 대기

- [x] 매칭 기회 횟수 — 베타에서는 1회가 정책이라 `count={1}` 고정이 맞다. 회차가 늘어날 때 서버 필드를 요청한다
- [x] 활성 방이 있는 사용자의 참여를 미리 막는다 — `myRoom.teamSide === "CREATOR" && status === "OPEN"`이면 방 상세 시트의 참여 버튼을 잠근다
- [ ] 매칭까지 끝난 방을 들고 있어도 서버가 참여를 막는지 확인 — 스펙의 `ACTIVE_ROOM_EXISTS` 설명이 "생성한 방 진행 중"이라 확실한 경우만 미리 막았다. 다만 매칭된 방을 들고 있으면 `creationEligibility.reason`이 `ACTIVE_ROOM_EXISTS`로 온다고 보고 있어(`lobby/page.tsx`) 참여도 막힐 가능성이 크다. 막힌다면 `getJoinBlockedReason`에 그 조건을 넣는다
- [ ] 만료 시점에 로비에 없던 사용자에게 소멸 시트를 보여줄 방법 — 만료된 방은 `myRoom`에서 빠지기만 해서 판정 근거가 없다. 서버가 EXPIRED를 한 번 내려주거나 클라이언트가 방 정보를 영속 저장해야 한다
- [ ] 서버가 주는 연락처 형태 확인 — 클라이언트는 `@핸들`과 `010…`만 보내므로(`ContactStep`, `MemberForm`) 서버가 저장·정규화 과정에서 URL로 바꾸지만 않으면 된다. 바꾼다면 `ProfileCard`의 인스타 판별(`@` 접두사)이 어긋난다
- [ ] 매칭된 방의 `expiresAt` 의미 확인 — 지금은 방 수명(1시간)이 지나면 클라이언트가 매칭 시트를 내린다. 서버가 끝난 방도 `myRoom`에 유지한다면 사용자는 활성 방 때문에 생성이 막힌 채 아무것도 못 보게 된다

### 기타

- [x] Wolf 3D 아이콘 교체 — Figma `2425:11313`을 다른 동물과 같은 540x378로 잘라 `animals/male/wolf.png`에 넣었다
